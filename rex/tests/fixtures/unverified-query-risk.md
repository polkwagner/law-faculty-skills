# Data Access Change

The patch changes a query helper, but the fixture provides no database schema, query implementation, execution plan, or benchmark. The review must not state that it creates an N+1 query as fact.
